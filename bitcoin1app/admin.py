# from django.contrib import admin
# from .models import Bitcoin
#
# class BitcoinAdmin(admin.ModelAdmin):
#     list_display = ['name', 'price']  # Omit any date fields you don't want to display
#     fieldsets = (
#         (None, {'fields': ('name', 'price')}),
#         ('Important Dates', {'fields': ('created_date', 'updated_date')}),
#     )
#
# admin.site.register(Bitcoin, BitcoinAdmin)
